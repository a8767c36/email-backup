This app creates a backup of your emails.

Usage: `node app.js *host* *port* *username* *password* *output.db*`.  
`host`, `port`, `username`, `password` of your IMAP server.  
`output.db` is the output SQLite3 file.
